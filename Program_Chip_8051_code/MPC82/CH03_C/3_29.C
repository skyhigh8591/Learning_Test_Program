/************* 3_29.c ************************
*動作：P2及P3輸入相比較，較大送入P1，較小送入P0 
**********************************************/
#include "..\MPC82.H"  //暫存器及組態設定	
main()
{
     unsigned char i,j;  //定義8-bit變數
 loop:
   i=P2; j=P3;        //P2,P3 輸入   
   P1=(i>j) ? i:j ; //找出兩變數的較大值，由P1輸出
   P0=(i<j) ? i:j ; //找出兩變數的較小值，由P0輸出
  goto loop;
}