/************* 3_26.c ******************
*動作：應用if-else，令LED=0~10重覆計數，由LED輸出
*硬體：SW1-3(P0LED)ON
*****************************************/
#include "..\MPC82.H"  //暫存器及組態設定
main()
{
    char i=0;//設定計數
    P0M0=0; P0M1=0xFF; //設定P0為推挽式輸出(M0-1=01)
  loop:
    LED=~i;	 //i輸出
    Delay_ms(100);
    if(i<10) i++; //假如i<10，則i遞加	 
      else   i=0; //假如i=10，則i=0
   goto loop;
}
