;
; 配合"ADC0804實習板"
;   讀取ADC0804的類比電壓值(採用查詢方式來同步),並將之顯示於8個LED上
;
ADCRD	EQU	P1.0
ADCWR	EQU	P1.1
ADCINTR	EQU	P3.2

START:
	CLR	ADCWR
	SETB	ADCWR
	JB	ADCINTR,$
	CLR	ADCRD
	MOV	P2,P0
	SETB	ADCRD
	SJMP	START
	END
