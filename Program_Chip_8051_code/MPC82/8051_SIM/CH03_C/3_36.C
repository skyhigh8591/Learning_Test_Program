/************* 3_36.c ***************************
*動作：令LED左移到bit7=1時，停止
*硬體：SW1-3(P0LED)ON
************************************************/
#include "..\MPC82.H"  //暫存器及組態設定
main()
{
  unsigned char i=0x01;
  P0M0=0; P0M1=0xFF; //設定P0為推挽式輸出(M0-1=01)
  while (i<0x80)  //符合條件繼續左移到bit7=1
   {
     LED=~i; Delay_ms(100);
     i=i<<1 ;
   }
  while(1); //移到bit7=1時，自我空轉
}