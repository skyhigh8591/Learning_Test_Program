;
;    用單相激磁的方式,使步進馬達反轉n格
;
N	EQU	10
START:
	MOV	R0,#0
	MOV	R1,#00001000B
LOOP:
	MOV	P0,R1
	MOV	A,R1
	RR	A
	CJNE	A,#10000000B,NEXT1
	MOV	A,#00001000B
NEXT1:
	MOV	R1,A
	ACALL	DELAY10MS
	INC	R0
	CJNE	R0,#N+1,LOOP
	AJMP	$
	$include (delay.inc)
	END
