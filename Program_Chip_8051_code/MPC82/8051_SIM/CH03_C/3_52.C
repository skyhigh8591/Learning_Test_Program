/*********** 3_52.c*****for迴圈中斷************
*動作：LED輸出1~7，若超過則停止
*硬體：SW1-3(P0LED)ON
**********************************************/
#include "..\MPC82.H"  //暫存器及組態設定
main()
{
  char  i;		      //宣告計數
  P0M0=0; P0M1=0xFF; //設定P0為推挽式輸出(M0-1=01)
  for(i=1;i<=100;i++) //設定的條件無作用
   {
     if(i>7) break;  //假如i>7則中斷跳出迴圈  
	 LED=~i;     //計數由LED輸出
     Delay_ms(500);
   }
  while(1);           //自我空轉
} 