;
; 配合"ADC0804實習板"
;   讀取ADC0804的類比電壓值(採用延遲方式來同步),並將之顯示於8個LED上
;
ADCRD	EQU	P1.0
ADCWR	EQU	P1.1

START:
	CLR	ADCWR
	SETB	ADCWR
	ACALL	DELAY150US
	CLR	ADCRD
	MOV	P2,P0
	SETB	ADCRD
	SJMP	START
DELAY150US:
	MOV	R1,#74
	DJNZ	R1,$
	RET
	END
