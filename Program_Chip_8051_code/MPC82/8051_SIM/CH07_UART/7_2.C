/********** 7_2.c *******MODE0串列接收**************
* 動作：以MODE0串列接收字元資料，由LED顯示出來
******************************************************/
#include "..\MPC82.H"   //暫存器及組態定義
main()
{
   //AUXR2=URM0X6;	//鮑率=Fosc/2(軟體模擬無效)
   SCON=0x10;  /*0001 0000,設定串列埠為模式0及致能接收
                 bit7-6:mode=00,串列埠為模式0
		  		 bit4:REN=1,致能接收*/ 
   while(1)       //重覆執行
   {
    while(RI==0); //若RI=0表示未接收完畢，再繼續檢查
    RI=0;         //若RI=1表示已接收1個字元完畢，清除RI=0
    LED=SBUF;     //將接收到的字元由LED輸出
   }
}
