/************* 3_48.c ************************
*動作：令由LED=1~7輸出，若超過則停止
*硬體：SW1-3(P0LED)ON
**********************************************/
#include "..\MPC82.H"  //暫存器及組態設定
main()
{
  char  i=1;   //宣告計數初值
  P0M0=0; P0M1=0xFF; //設定P0為推挽式輸出(M0-1=01)
  do
    {
      LED=~i;   //計數由LED輸出 
      Delay_ms(100); //延時
      i++;     //計數遞加
    }
  while(i<8); //若計數<8，則跳到do執行
  while(1);    //若計數=8，自我空轉
}
