//*********LED.C*********專案程式執行********************
//功能︰令變數遞加由P0及P1輸出到LED
//操作：SW1-3(P0LED)及SW1-4(P1LED) ON
//********************************************************
#include "..\MPC82.H"   //暫存器及組態設定
main() //主程式
{      //主程式開始   
     unsigned char  i=0;  //計數變數0~255
	 P0M0=0; P0M1=0xFF; //設定P0為推挽式輸出(M0-1=01)
   loop:         	//標記名稱
     P0=P1=~i;      //變數i反相送到P0及P1輸出(設中斷點)
     Delay_ms(100); //延時函數式
     i=i+1;      	//變數加一  
     goto loop;  	//跳到標記loop
}	   //主程式結束