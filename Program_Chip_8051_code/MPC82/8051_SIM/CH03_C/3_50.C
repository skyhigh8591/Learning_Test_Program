/***** 3_50.c ***while迴圈中斷*****************
*動作：LED輸出1~7，若超過則停止
*硬體：SW1-3(P0LED)ON
*********************************************/
#include "..\MPC82.H"  //暫存器及組態設定
main()
{
  char  i=1;   //宣告計數初值
  P0M0=0; P0M1=0xFF; //設定P0為推挽式輸出(M0-1=01)
  while(1)     //重覆執行
  {
    LED=~i;   //計數由LED輸出
    Delay_ms(500);
    i++ ;    //計數遞加
    if(i>7) break;  //若i>7跳出迴圈
  }
  while(1);  //自我空轉
}