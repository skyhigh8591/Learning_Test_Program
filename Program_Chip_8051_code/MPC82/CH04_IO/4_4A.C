/********** 4_4A.C ****************************
*動作：由KEY1按鍵開關輸入，令LED遞加輸出
*硬體：SW1-3(P0LED)ON, 按KEY1 
***********************************************/
#include "..\MPC82.H"   //暫存器及組態定義 
main() 
{
   unsigned char i=0;
   P0M0=0; P0M1=0xFF; //設定P0為推挽式輸出(M0-1=01)
   while(1) //重覆執行 
   {
	LED=~i++;   //LED遞加
	while(KEY1==1); //若未按鍵，KEY1=1空轉
		Delay_ms(1);
	while(KEY1==0); //若未放鍵，KEY1=0空轉
		Delay_ms(1);
 
   }
} 