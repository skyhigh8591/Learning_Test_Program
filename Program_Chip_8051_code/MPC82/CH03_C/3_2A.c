//*********** 3_2A.c **********************************
//*動作：定義特殊功能暫存器
//*硬體：SW1-3(P0LED)ON,SW1-4(P1LED)ON
//******************************************************
#include "..\MPC82.H" //暫存器及組態設定
sfr  L0=0x80; //變數L0定義為SFR的P0埠0(0x80)
sfr  L1=0x90; //變數L1定義為SFR的P1埠0(0x90)
main()
{
   unsigned char i=0; //宣告8-bit無符號變數
   P0M0=0; P0M1=0xFF; //設定P0為推挽式輸出(M0-1=01)
  loop:
   L0=L1=~i; //變數反相輸出
   Delay_ms(100);
   i=i+1;         //變數遞加
  goto loop;
}