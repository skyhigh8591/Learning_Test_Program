/************** 3_25.c **************************
*動作：用if和goto指令，由LED輸出00~99十進制變化
*硬體：SW1-3(P0LED)ON
***********************************************/
#include "..\MPC82.H"  //暫存器及組態設定
main()
{       
   char i=12; //宣告計數變數
   P0M0=0; P0M1=0xFF; //設定P0為推挽式輸出(M0-1=01)
 loop:
   LED=~((i/10<<4)+(i%10)); //十進位計數輸出
   Delay_ms(500);
   i++;                 //計數加一
   if(i<100) goto loop; //若計數小於100到loop處   
   i=0;                 //若若計數等於100，令計數=0
   goto loop; 
}