/***** DOT1.c ******8*8點矩陣固定字型範例*****
*動作：8*8點矩陣LED由右到左掃瞄輸出
*接線：Scan低電位掃描輸出，由Data低電位輸出資料
*硬體：SW1-2(LED8X8)ON
************************************************/
#include "..\MPC82.H"   //暫存器及組態定義
main() 
{ 
  P0M0=0; P0M1=0xFF; //設定P0為推挽式輸出(M0-1=01)
  Scan=0x7F;       //由com1開始掃瞄輸出  
  while(1)		  
  {
     Data=0x00;         //整行全亮
     Delay_ms(500);    //延時
     Scan=RR8(Scan); //換掃瞄下一行
  } 
} 