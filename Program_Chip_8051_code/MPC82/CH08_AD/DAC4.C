/*******DAC4.C*****使用列表法輸出三相波形*******************
*功能：讀取正弦列表資料，由P0~P2送三相波形。
***********************************************************/
#include  "..\MPC82.H"
#include  "TABLE.H"	 // 正弦波列表資料 
int u,v,w;   //三相波形角度
main()
{
	while(1)  //重覆執行 
	{
	  for(u=0;u<360;u++)		 //u相波形角度
		{
			v=u+120; if(v > 359) v=v-360; //v相波形角度
			w=u+240; if(w > 359) w=w-360; //w相波形角度
			P0=sin_table[u];
			P1=sin_table[v];
			P2=sin_table[w];
		}
	}
}
